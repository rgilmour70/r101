// eslint-disable-next-line
import React, { Component } from 'react';
//import renderHTML from 'react-render-html';

const Declarative = (props) => {

	return <div className="text">{props.content.text}</div>

}

export default Declarative;